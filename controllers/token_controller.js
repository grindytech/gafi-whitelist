const dotenv = require("dotenv");
dotenv.config();

module.exports = {
    createToken: async function (req, res) {
        // create new token
    },

    transfer: async function (req, res) {

    },
};